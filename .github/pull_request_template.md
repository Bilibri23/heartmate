## Summary

- 

## Test plan

- [ ] `.\mvnw.cmd test` (or targeted tests)
- [ ] `npm run lint && npm run build` (if frontend touched)
- [ ] Flyway migration tested / `MigrationCoverageTest` updated (if schema changed)

## Deploy notes

- [ ] Railway Flyway will run on deploy (if migration added)
- [ ] Env var changes documented in `deploy/railway.env.example`
