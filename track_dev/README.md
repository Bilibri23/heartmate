# track_dev — historical journal

> **Historical development journal from early sprints.** For current stack, ports, and deploy, use the root [README.md](../README.md) and [docs/deploy/production-deploy.md](../docs/deploy/production-deploy.md).

This folder may reference outdated tooling (Vite, Render, RoomConnect naming, wrong ports). Do not use it as source of truth for AI agents or new features.
